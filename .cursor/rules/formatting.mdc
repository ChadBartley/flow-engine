---
alwaysApply: true
---

# Formatting

Before committing, run `./fmt.sh` from the workspace root to format all code:

- **Rust**: `cargo fmt --all` + `cargo clippy --all --fix --allow-dirty`
- **Python** (`crates/cleargate-python/`): `ruff format` + `ruff check --fix`
